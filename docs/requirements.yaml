name: docs
channels:
dependencies:
    # Base depends
  - python
  - pip
  - numpy
  - pandas
  - pennylane
  - scikit-learn
  - trimap
  - pacmap
  - seaborn
  - matplotlib

    # Pip-only installs
  #- pip:

