# readthedocs.yml

version: 2

build:
  os: ubuntu-22.04
  image: latest

python:
  version: 3.11
  install:
    - method: pip
      path: .

sphinx:

  configuration: docs/conf.py


conda:
  environment: docs/requirements.yaml