# readthedocs.yml

version: 2

build:
  os: ubuntu-22.04
  # image: latest
  tools:
    python: "3.11"

sphinx:
  configuration: docs/conf.py


python:
  - version: 3.11
  install:
    - method: pip
      path: .

conda:
  environment: docs/requirements.yaml