# readthedocs.yml

version: 2

build:
  image: latest

python:
  version: 3.11
  install:
    - method: pip
      path: .

sphinx:
   configuration: docs/conf.py

conda:
  environment: docs/requirements.yaml