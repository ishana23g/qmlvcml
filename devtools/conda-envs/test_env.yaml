name: test
channels:
  - conda-forge
  - defaults

dependencies:
  # Base depends
  - python
  - pip

  # Testing
  - pytest
  - pytest-cov

  # Pip-only installs
  - pip:
    - codecov
    - numpy
    - pandas
    - pennylane
    - scikit-learn
    - trimap
    - pacmap
    - seaborn
    - matplotlib